#include "main.h"
/**
 * _puts_recursion - function like puts();
 * str: input
 * Return: Always 0 (Success)
 */

int main()
{
char str[] = "Hello, programming world!\n";
_puts_recursion(str);
return (0);
}
